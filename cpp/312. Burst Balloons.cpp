#include <cstdio>
#include <vector>
#include <cmath>

using namespace std;

class Solution {
public:
    int maxCoins(vector<int>& nums) {
        
    }
};

int main() {
    vector<int> nums = {3,1,5,8};
    printf("%d\n", Solution().maxCoins(nums));
    return 0;
}
